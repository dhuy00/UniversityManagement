# Legacy SQL

These scripts belong to the original generic Oracle admin console.

They are not part of the ordered university security installation and are not
included by any runner:

- `common_procedure.sql`
- `role_procedure.sql`
- `user_procedure.sql`
- `meta_data.sql`
- `test.sql`

Keep them only while the existing API still depends on their stored procedures. Refactor or remove them during the backend phase.
